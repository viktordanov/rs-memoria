[package]
name = "memoria-application"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Use cases, ports, and DTOs for Memoria."

[dependencies]
memoria-domain = { path = "../memoria-domain" }
